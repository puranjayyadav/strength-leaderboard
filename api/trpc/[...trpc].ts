// This file uses the pre-bundled API code from api/dist/index.js
// The bundling is done by npm run build:api which includes all dependencies
import handler from "../dist/index.js";

export default handler;
